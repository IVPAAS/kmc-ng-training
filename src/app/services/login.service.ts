import { Injectable } from '@angular/core';
import { BehaviorSubject } from 'rxjs';

@Injectable()
export class LoginService {    
    public Login( entryId: number, userName: string, password: string): boolean {
        
       return true;
    }
}

